package dev.chronoly.core.quest;

/** The four things every prophecy in the books turns out to have been about. */
public enum Slot {
    /** Who or what you must face. */
    TARGET,
    /** Where. */
    PLACE,
    /** What you must bring back. */
    RELIC,
    /** What it will cost you — the line everybody misreads until it is too late. */
    COST
}
